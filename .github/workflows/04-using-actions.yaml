name: 04 - Using Actions
on: push
jobs:
  build:
    runs-on: windows-latest
    steps:
        - name: Checkout code
          uses: actions/checkout@v4
        - name: Printing folders
          run: ls
    # Install deps of our react app
    # Execute automated tests